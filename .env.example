# Request settings
REQUEST_TIMEOUT=10
MAX_RETRIES=3

# User-Agent (override default)
# USER_AGENT="Mozilla/5.0 (compatible; MyScraper/1.0)"

# Output
DEFAULT_OUTDIR=output
DEFAULT_FORMAT=both
DEFAULT_LIMIT=30
